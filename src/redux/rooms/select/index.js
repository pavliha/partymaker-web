import guests from '../guests/select'
import all from './all'
import current from './current'
import messages from '../messages/select'
import orders from '../../orders/select'

export default {
  messages,
  orders,
  guests,
  all,
  current
}
